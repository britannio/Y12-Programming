# Merge sort
import array_gen

unsorted = array_gen.random_array(length=15, upper=100, repeats=True)


def merge_sort(array):
    for x in range(len(array)):
        array = [[[],[]],[]]
        pass
    return array


print(merge_sort(unsorted))

